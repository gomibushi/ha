  - platform: history_stats
    name: Williams PC på i dag
    entity_id: binary_sensor.williamspc
    state: 'on'
    type: time
    start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
    end: '{{ now() }}'